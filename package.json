{
    "name": "undangan-khitanan",
    "version": "1.0.0",
    "description": "Digital Invitation for Khitanan Ahmad",
    "main": "server.js",
    "devDependencies": {
        "@eslint/js": "^9.19.0",
        "eslint": "^9.19.0",
        "fs-extra": "^11.3.0",
        "globals": "^15.14.0",
        "htmlhint": "^1.1.4",
        "madge": "^8.0.0",
        "stylelint": "^16.6.1",
        "stylelint-config-recommended": "^14.0.0"
    },
    "scripts": {
        "start": "node server.js",
        "dev": "node dev-server.js",
        "dev:server": "node server.js",
        "dev:build": "npx esbuild js/*.js --bundle --outdir=dist --watch",
        "lint:js": "eslint js",
        "lint:css": "stylelint css",
        "lint:html": "htmlhint **/*.html",
        "build": "npx esbuild js/*.js --bundle --minify --outdir=dist",
        "build:public": "npm run build && node -e \"const fs = require('fs-extra'); fs.ensureDir('public').then(() => Promise.all(['assets', 'css', 'dist', 'index.html', 'dashboard.html'].map((f) => fs.copy(f, 'public/' + f)))).then(() => console.log('✅ Successfully!'));\""
    },
    "dependencies": {
        "express": "^4.19.2",
        "cors": "^2.8.5",
        "body-parser": "^1.20.2",
        "fs-extra": "^11.3.0",
        "esbuild": "^0.24.2",
        "uuid": "^9.0.1",
        "helmet": "^7.1.0",
        "compression": "^1.7.4"
    }
}